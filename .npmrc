# npm 配置文件

# 发布配置
# 取消注释以启用严格的 SSL 检查
# strict-ssl=true

# 取消注释以启用发布前确认
# npm-publish-confirm=true

# 注册表配置（默认使用 npm 官方源）
registry=https://registry.npmjs.org/

# 可选：如果需要使用国内镜像进行安装（仅用于 install，不影响 publish）
# registry=https://registry.npmmirror.com/

